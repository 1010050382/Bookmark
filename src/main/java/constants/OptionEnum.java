package constants;

public enum OptionEnum {
    exit,
    show_tree,
    ls_tree,
    add_title,
    at,
    add_bookmark,
    delete_title,
    delete_bookmark,
    open_file,
    create_file,
    edit_file,
    save,
    undo,
    redo,
    read_bookmark
}
